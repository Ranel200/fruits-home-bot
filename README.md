# Фрукты Дома - Telegram Mini App

Мини-приложение для Telegram бота с каталогом фруктов, корзиной и оформлением заказов.

## 🚀 Быстрый старт

### Локальная разработка

**Установка зависимостей:**
```bash
npm install
```

**Запуск:**
```bash
npm start
```

Сервер будет доступен на `http://localhost:3000`

## 📦 Зависимости

Все библиотеки указаны в `package.json`:

**Основные:**
- `express` - веб-сервер
- `cors` - обработка CORS запросов
- `body-parser` - парсинг тела запросов

**Для разработки:**
- `nodemon` - автоперезагрузка при изменениях

## 📁 Структура проекта

- `server.js` - Express сервер с API
- `package.json` - зависимости проекта
- `vercel.json` - конфигурация для Vercel
- `public/index.html` - главная страница
- `public/styles.css` - стили
- `public/app.js` - логика приложения

## 🔌 API Endpoints

- `GET /api/fruits` - Получить все фрукты
- `GET /api/fruits/:id` - Получить фрукт по ID
- `POST /api/orders` - Создать заказ

## ✨ Функционал

- Каталог фруктов с категориями
- Поиск по названию
- Корзина с добавлением/удалением товаров
- Оформление заказов
- Интеграция с Telegram Web App API
- Адаптивный дизайн


## 🤖 Подключение к Telegram

### Вариант 1: Деплой на Vercel (рекомендуется)

1. **Подготовка к деплою:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```
   
2. **Создайте репозиторий на GitHub** и отправьте код

3. **Деплой на Vercel:**
   - Зайдите на https://vercel.com
   - Подключите GitHub репозиторий
   - Нажмите Deploy
   - Скопируйте URL (например: `https://your-app.vercel.app`)

4. **Настройка в @BotFather:**
   - `/newapp` → выберите бота
   - Web App URL: вставьте URL от Vercel

📖 **Подробная инструкция:** см. `DEPLOY_QUICK.md` или `VERCEL_DEPLOY.md`