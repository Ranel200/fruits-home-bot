# Настройка Telegram бота

## Шаг 1: Получите токен бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot` (если бота еще нет) или `/token` (если бот уже создан)
3. Следуйте инструкциям и скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройте Mini App в BotFather

1. Откройте **@BotFather**
2. Отправьте `/newapp` или `/myapps`
3. Выберите вашего бота
4. Заполните:
   - **Title**: Фрукты Дома
   - **Web App URL**: `https://fruits-home-bot.vercel.app` (или ваш URL)
   - **Short name**: fruits

## Шаг 3: Запустите бота

### Вариант 1: Локально

1. Создайте файл `.env` в корне проекта:
   ```
   BOT_TOKEN=ваш_токен_бота
   WEB_APP_URL=https://fruits-home-bot.vercel.app
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Запустите бота:
   ```bash
   npm run bot
   ```

### Вариант 2: На сервере (VPS, Railway, Render и т.д.)

1. Добавьте переменные окружения:
   - `BOT_TOKEN` - токен вашего бота
   - `WEB_APP_URL` - URL вашего Mini App

2. Запустите бота:
   ```bash
   npm run bot
   ```

## Что делает бот:

- При команде `/start` отправляет приветственное сообщение "Добро пожаловать в мир фруктов" с кнопкой "Заказать фрукты"
- Кнопка открывает ваше Mini App
- При любом текстовом сообщении предлагает открыть каталог

## Проверка работы:

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Должно появиться сообщение с кнопкой "🍎 Заказать фрукты"
4. Нажмите на кнопку - откроется ваше Mini App

---

**Важно:** Бот должен работать постоянно, чтобы отвечать на сообщения. Для продакшена используйте VPS или облачные сервисы (Railway, Render, Heroku и т.д.)
